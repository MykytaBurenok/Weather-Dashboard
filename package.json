{
  "name": "weather-dashboard",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "acp": "git add --all && git commit -m \"update\" && git push"
  },
  "devDependencies": {
    "vite": "^5.4.6"
  }
}
